{
  "name": "@rattlr/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and protocol-adapter interface used by the indexer, api, and bot.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint not configured yet)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

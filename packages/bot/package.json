{
  "name": "@rattlr/bot",
  "version": "0.0.0",
  "private": true,
  "description": "Telegram + Discord alert bot. Sends a message when a subscribed position crosses its configured threshold.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint not configured yet)\""
  },
  "dependencies": {
    "@rattlr/core": "workspace:*",
    "telegraf": "^4.16.3",
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.7.5"
  }
}

{
  "name": "@rattlr/api",
  "version": "0.0.0",
  "private": true,
  "description": "Serves dashboard data and manages alert-threshold subscriptions.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint not configured yet)\""
  },
  "dependencies": {
    "@rattlr/core": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.7.5"
  }
}
